name: Release to PyPI

on:
  release:
    types: [published]

jobs:
  validate:
    name: Validate build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6

      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6
        with:
          python-version: "3.12"

      - name: Verify tag matches package version
        env:
          TAG_REF: ${{ github.ref_name }}
        run: |
          python - <<'EOF'
          import re, pathlib, sys, os
          tag = os.environ["TAG_REF"].lstrip("v")
          text = pathlib.Path("any2md/__init__.py").read_text()
          m = re.search(r'__version__\s*=\s*["\x27]([^"\x27]+)["\x27]', text)
          if not m:
              print("::error::Could not find __version__ in any2md/__init__.py")
              sys.exit(1)
          version = m.group(1)
          if tag != version and not tag.startswith(version + "-"):
              print(f"::error::Tag {tag!r} != __version__ {version!r}")
              sys.exit(1)
          print(f"Version OK: {version} (tag: {tag})")
          EOF

      - name: Install build tools
        run: pip install build twine

      - name: Build sdist and wheel
        run: python -m build

      - name: Validate distribution metadata
        run: twine check dist/*

      - name: Upload dist artifacts
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f  # v6
        with:
          name: dist
          path: dist/

  smoke:
    name: Smoke test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6

      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6
        with:
          python-version: "3.12"

      - name: Install package
        run: pip install -e .

      - name: Smoke test
        run: python -c "import any2md; print(f'any2md {any2md.__version__} OK')"

  publish-testpypi:
    name: Publish to TestPyPI
    needs: [validate, smoke]
    if: github.event.release.prerelease == true
    runs-on: ubuntu-latest
    environment: testpypi
    permissions:
      id-token: write
    steps:
      - name: Download dist artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: dist
          path: dist/

      - name: Publish to TestPyPI
        uses: pypa/gh-action-pypi-publish@cef221092ed1bacb1cc03d23a2d87d1d172e277b  # release/v1
        with:
          repository-url: https://test.pypi.org/legacy/
          attestations: false

  publish-pypi:
    name: Publish to PyPI
    needs: [validate, smoke]
    if: github.event.release.prerelease == false
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - name: Download dist artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: dist
          path: dist/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@cef221092ed1bacb1cc03d23a2d87d1d172e277b  # release/v1
        with:
          attestations: false
